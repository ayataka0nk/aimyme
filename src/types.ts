export type User = {
  id: string
  name: string
  email: string
}

export type ProjectSummary = {
  id: string
  name: string
  description: string
  ownerUserId: string
}

export type ProjectDetail = {
  id: string
  name: string
  description: string
  ownerUserId: string
}
